string is platte tekst.

Integer is een single digit nummer (Kan geen decimalen hebben).

float of doubles kunnen wel wel decimalen bevatten maar kost meer rekenkracht dan een int.

boolean is een true of false statement. wordt gebruikt om condities te checken.

Arrays zijn een verzameling van meerdere values (of een verzameling variables met de values van de andere variables) in één variable.

NULL is leegte, de var heeft geen waarde waar je wat mee kan doen.

Object is een instance gemaakt van de variables die bestaan in een class functie.
instance dus <<
betekent dat je meerdere objects kan maken met varierende waardes uit dezelfde class.. right?

